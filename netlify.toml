[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js plugin for Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Functions directory
[functions]
  directory = "netlify/functions"

# Schedule is defined in the function file via config export
